# =================================================================
#               Blackbox Project .gitignore
# =================================================================

# 1. 빌드 결과물 (Build artifacts)
# 컴파일러가 소스 코드를 기반으로 자동으로 생성하는 모든 파일을 무시합니다.
# 이 파일들은 소스 코드만 있으면 언제든지 다시 만들 수 있으므로 Git에 올릴 필요가 없습니다.
build/
*.o
*.so

# 2. Python 캐시 파일 (Python cache)
# Python 인터프리터가 실행 속도를 높이기 위해 자동으로 생성하는 파일들입니다.
# 개발 환경마다 내용이 다를 수 있어 공유하지 않습니다.
__pycache__/
*.pyc

# 3. 최종 실행 파일 (Final executables)
# Makefile에 의해 build/bin 안에 생성되지만, 만약을 위해 추가합니다.
blackbox_main
run_test
test_popen

# 4. 임시 테스트 파일 (Temporary test files)
# 디버깅을 위해 만들었던 임시 파일들은 버전 관리 대상에서 제외합니다.
test.raw
test_popen.c
minimal_test.py

# 5. OS 및 에디터 자동 생성 파일 (OS & Editor specific files)
# 다양한 운영체제나 텍스트 에디터가 자동으로 생성하는 임시 파일들을 무시합니다.
.DS_Store
*~
*.swp
*.swo